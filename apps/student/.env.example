# Vite portal dev server for same-origin `/hub` + portal `/api/*` rewrites (default: http://127.0.0.1:5174)
# PORTAL_PROXY_URL=http://127.0.0.1:5174
#
# Only for split deploy: absolute URL of the portal (otherwise links use /hub on this origin)
# NEXT_PUBLIC_PORTAL_URL=
#
# Optional — if set, POST /api/generate tries one Groq chat completion first, then
# always falls back to the built-in deterministic engine when the call fails
# or when this key is unset.
GROQ_API_KEY=
# Optional model (Groq; default in code: llama-3.3-70b-versatile)
# GROQ_MODEL=
