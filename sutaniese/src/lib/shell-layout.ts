/**
 * Full-bleed shell on large screens: no max width, only horizontal insets
 * (header, main, footer, bottom bar).
 */
export const SHELL_PX =
  "w-full min-w-0 px-4 sm:px-6 md:px-8 lg:px-10 xl:px-12 2xl:px-16" as const;
