# Optional — if set, POST /api/generate tries one Anthropic JSON call first, then
# always falls back to the built-in deterministic engine when the call fails
# or when this key is unset.
ANTHROPIC_API_KEY=
# Optional model override (default: claude-3-5-sonnet-20241022 in code)
# ANTHROPIC_MODEL=
