# Локальный `npm run dev` — Gemini через Vite middleware (ключ не в клиентском бандле).
# Используется для: /api/career-compare, /api/recommendation-letter
GEMINI_API_KEY=
# Необязательно; по умолчанию gemini-3-flash-preview
# GEMINI_MODEL=
